function CrossArray(Array1, Array2) {
    let Array3 = Array1;
    for (i=0, j=0; i<Array3.length, j<Array2.length; i++){  
function unique(arr) {
  return Array.from(new Set(arr));
}
unique(Array3)
    return Array3;
  }
}
    console.log(CrossArray(arr, arr2));